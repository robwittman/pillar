http_addr: ":8080"
grpc_addr: ":9090"
postgres_url: "postgres://pillar:pillar@localhost:5432/pillar?sslmode=disable"
redis_addr: "localhost:6379"
log_level: "debug"

plugin_settings:
  cache_dir: ~/.pillar/plugins

auth:
  enabled: true
  session_secret: "qpXpuvo5lUXM3F6ijQc+2OoOJS8yRQAnFoXwHZtYmX0="
  session_ttl: "24h"
  allow_signup: true
  providers:
    - type: local
      name: local

plugins:
#  - name: keycloak
#    path: ./bin/example-plugin-keycloak
#    config:
#      realm: agents
#      admin_url: https://${KEYCLOAK_HOST:-localhost:8443}/auth
#      client_id: ${KC_CLIENT_ID:-pillar-admin}
#      client_secret: ${KC_CLIENT_SECRET}
